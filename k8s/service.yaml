apiVersion: v1
kind: Service
metadata:
  name: order-api-service
spec:
  type: NodePort
  selector:
    app: order-api
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000
      nodePort: 30050  # This maps to http://localhost:30050 on your browser!